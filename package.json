{
  "name": "gain",
  "version": "0.1.0",
  "private": true,
  "description": "The structured middle of a copy-paste loop between AI-authored training plans and the person following them.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check:chars": "if git ls-files -z '*.ts' '*.js' '*.md' '*.json' '*.yml' '*.yaml' '*.css' '*.html' '*.svelte' | xargs -0 grep -laP '[\\x00-\\x08\\x0B\\x0C\\x0E-\\x1F\\x7F]'; then echo 'Literal control characters in the files above. Write them as escapes (\\u0000) — a raw one makes git treat the file as binary, and a file with no reviewable diff is a file nobody reviews.' >&2; exit 1; fi",
    "verify": "npm run typecheck && npm run lint && npm run check:chars && npm run format:check && npm test"
  },
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "ulidx": "^2.4.1",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.66.0",
    "vitest": "^4.1.10"
  }
}
