# Dependencies
node_modules/

# Build output
build/
dist/
.svelte-kit/
coverage/

# Environment and secrets
.env
.env.*
!.env.example

# Runtime data — never commit user training data
data/
*.db
*.db-shm
*.db-wal

# Editor and OS
.DS_Store
*.swp
*~

# Personal agent overrides. .claude/settings.json IS committed — it carries the shared
# hook — but local overrides and MCP auth are per-developer.
.claude/settings.local.json
